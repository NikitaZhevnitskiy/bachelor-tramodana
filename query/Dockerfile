FROM openjdk:8

EXPOSE 5555
EXPOSE $TRAMODANA_QUERY_PORT
ADD build/install /opt
ENV PATH=/opt/query/bin:$PATH
#ENV TRAMODANA_QUERY_HTTP_PORT=5555
#ENV KAFKA_BOOTSTRAP_SERVERS="localhost:9092"

CMD ["query"]