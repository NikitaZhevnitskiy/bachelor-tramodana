package no.sysco.middleware.tramodana.modeler;

import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

public class BpmnModelCreatorTest {

    @Before
    public void setUp() {
    }

    @After
    public void tearDown() {
    }

    @Test
    public void createTestBpmnDiagramWithFluentAPI() {
        Assert.assertTrue(true);
    }

    @Test
    public void createTestBpmnProcessProcedurally() {
        Assert.assertTrue(true);
    }

    @Test
    public void parseToBpmn() {
        Assert.assertTrue(true);
    }
}